import React from 'react';

import './LayoutHeader.css';

const LayoutHeader: React.FC = () => {
  return (
    <header>
      <nav>need LayoutHeader here</nav>
    </header>
  );
};

export default LayoutHeader;
